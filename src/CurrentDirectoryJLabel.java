import java.io.File;
import java.util.Observable;
import java.util.Observer;
import javax.swing.JLabel;


/**
 * Displays the currently selected directory
 * @author Rob
 *
 */
/*
@SuppressWarnings("serial")
public class CurrentDirectoryJLabel extends JLabel implements Observer
{
	public CurrentDirectoryJLabel()
	{
		super("None Selected");
	}

	@Override
	public void update(Observable currentDirMgr, Object ObjNewDir)
	{
		File newDir = (File) ObjNewDir;
		setText(newDir.getAbsolutePath());
		

	}

}
*/